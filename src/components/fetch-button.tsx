import React from 'react';
import { Button } from "@material-ui/core";

const FetchButton = () => {
  return (
    <Button
      id="customTest"
      type="button"
      variant="contained"
      color="primary"
    >
      Fetch
    </Button>
  );
};

export default FetchButton;
